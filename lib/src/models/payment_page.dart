/// Payment page model.
library;

/// Payment page configuration.
class PaymentPage {
  /// Page title.
  final String title;
  
  /// Page description.
  final String description;

  const PaymentPage({
    required this.title,
    required this.description,
  });
}